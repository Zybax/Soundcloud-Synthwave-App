import React from 'react'

// This component will be use for all level 1 title in the page (h1)
export default function Title(props) {
  return (
    <div>
      <h1>{this.props.title}</h1>
    </div>
  )
}
